//collecting all the non zeros that will go at the end
//and put them in temp
//then all the remaining places will be zero
//first iterating then temp then zeros
//temp.add(arr[i]); if (arr[i]!=0) temp.add(arr[i]);
//inner loop fopr non zero for(i =nonzero;i<n;i
//++){
//arr[i]=0;
//
//O(N)
//in the inner loop we are starting uit fromwhere the non zero is finised
//2nd method we can check if it is 0 or not and if it is zero then we will swap it to the next one
